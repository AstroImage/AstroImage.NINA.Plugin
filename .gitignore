# compilazione
bin/
obj/
*.user
*.suo
.vs/

# pacchetti
packages/
*.nupkg

# il motore di AstroImage-Strategy NON deve MAI finire qui dentro.
# Questo repository e' pubblico e con licenza MPL-2.0 perche' il catalogo di N.I.N.A. non
# accetta plugin a sorgente chiuso; il motore, il catalogo curato e i dati fotometrici
# restano nel loro. Le righe qui sotto non sono un promemoria: sono una rete.
index.html
data/
*.strategy.json

# i nomi delle macchine di chi installa: il modello e' scripts/installa.esempio.psd1
scripts/installa.locale.psd1

# uscite del banco di prova
TestResults/
*.trx
coverage.cobertura.xml

# il pacchetto da mandare, che scripts/pacchetto.ps1 rifa' a ogni giro
pacchetto/
